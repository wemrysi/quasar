# This file was automatically generated by sbt-github-actions using the
# githubWorkflowGenerate task. You should add and commit this file to
# your git repository. It goes without saying that you shouldn't edit
# this file by hand! Instead, if you wish to make changes, you should
# change your sbt build configuration to revise the workflow description
# to meet your needs, then regenerate this file.

name: Continuous Integration

on:
  pull_request:
    branches: [master, backport/v*]
    types: [opened, reopened, synchronize, ready_for_review]
  push:
    branches: [master, backport/v*]

env:
  SBT: ./sbt
  REPO_SLUG: ${{ github.repository }}
  ENCRYPTION_PASSWORD: ${{ secrets.ENCRYPTION_PASSWORD }}
  GITHUB_ACTOR: precog-bot
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    name: Build and Test
    if: '!(github.event_name == ''pull_request'' && github.event.pull_request.draft)'
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.12.10]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (fast)
        uses: actions/checkout@v2

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v5
        with:
          java-version: ${{ matrix.java }}

      - name: Cache ivy2
        uses: actions/cache@v1
        with:
          path: ~/.ivy2/cache
          key: ${{ runner.os }}-sbt-ivy-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (generic)
        uses: actions/cache@v1
        with:
          path: ~/.coursier/cache/v1
          key: ${{ runner.os }}-generic-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (linux)
        if: contains(runner.os, 'linux')
        uses: actions/cache@v1
        with:
          path: ~/.cache/coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (macOS)
        if: contains(runner.os, 'macos')
        uses: actions/cache@v1
        with:
          path: ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (windows)
        if: contains(runner.os, 'windows')
        uses: actions/cache@v1
        with:
          path: ~/AppData/Local/Coursier/Cache/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache sbt
        uses: actions/cache@v1
        with:
          path: ~/.sbt
          key: ${{ runner.os }}-sbt-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Common sbt setup
        if: env.ENCRYPTION_PASSWORD != null
        run: $SBT ++${{ matrix.scala }} transferCommonResources exportSecretsForActions

      - name: Check that workflows are up to date
        run: $SBT ++${{ matrix.scala }} githubWorkflowCheck

      - run: $SBT ++${{ matrix.scala }} ci

      - name: Upload target directory 'sql/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-sql_target
          path: sql/target

      - name: Upload target directory 'qsu/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-qsu_target
          path: qsu/target

      - name: Upload target directory 'target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-target
          path: target

      - name: Upload target directory 'impl/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-impl_target
          path: impl/target

      - name: Upload target directory 'qscript/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-qscript_target
          path: qscript/target

      - name: Upload target directory 'foundation/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-foundation_target
          path: foundation/target

      - name: Upload target directory 'common/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-common_target
          path: common/target

      - name: Upload target directory 'connector/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-connector_target
          path: connector/target

      - name: Upload target directory 'api/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-api_target
          path: api/target

      - name: Upload target directory 'ejson/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-ejson_target
          path: ejson/target

      - name: Upload target directory 'core/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-core_target
          path: core/target

      - name: Upload target directory 'frontend/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-frontend_target
          path: frontend/target

      - name: Upload target directory 'project/target'
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-project_target
          path: project/target

  publish:
    name: Publish Artifacts
    needs: [build]
    if: github.event_name != 'pull_request' && (github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/heads/backport/v'))
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.12.10]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (fast)
        uses: actions/checkout@v2

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v5
        with:
          java-version: ${{ matrix.java }}

      - name: Cache ivy2
        uses: actions/cache@v1
        with:
          path: ~/.ivy2/cache
          key: ${{ runner.os }}-sbt-ivy-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (generic)
        uses: actions/cache@v1
        with:
          path: ~/.coursier/cache/v1
          key: ${{ runner.os }}-generic-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (linux)
        if: contains(runner.os, 'linux')
        uses: actions/cache@v1
        with:
          path: ~/.cache/coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (macOS)
        if: contains(runner.os, 'macos')
        uses: actions/cache@v1
        with:
          path: ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (windows)
        if: contains(runner.os, 'windows')
        uses: actions/cache@v1
        with:
          path: ~/AppData/Local/Coursier/Cache/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache sbt
        uses: actions/cache@v1
        with:
          path: ~/.sbt
          key: ${{ runner.os }}-sbt-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Download target directory 'sql/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-sql_target
          path: sql/target

      - name: Download target directory 'qsu/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-qsu_target
          path: qsu/target

      - name: Download target directory 'target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-target
          path: target

      - name: Download target directory 'impl/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-impl_target
          path: impl/target

      - name: Download target directory 'qscript/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-qscript_target
          path: qscript/target

      - name: Download target directory 'foundation/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-foundation_target
          path: foundation/target

      - name: Download target directory 'common/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-common_target
          path: common/target

      - name: Download target directory 'connector/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-connector_target
          path: connector/target

      - name: Download target directory 'api/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-api_target
          path: api/target

      - name: Download target directory 'ejson/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-ejson_target
          path: ejson/target

      - name: Download target directory 'core/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-core_target
          path: core/target

      - name: Download target directory 'frontend/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-frontend_target
          path: frontend/target

      - name: Download target directory 'project/target'
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-project_target
          path: project/target

      - name: Install Ruby
        uses: actions/setup-ruby@v1
        with:
          ruby-version: 2.6

      - name: Common sbt setup
        run: $SBT ++${{ matrix.scala }} transferCommonResources transferPublishAndTagResources exportSecretsForActions

      - run: ./scripts/commonSetup

      - name: Publish artifacts and create tag
        run: ./scripts/publishAndTag ${{ github.repository }}

  auto-merge:
    name: Auto Merge
    needs: [build]
    if: github.event_name == 'pull_request' && contains(github.head_ref, 'version-bump')
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.12.10]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (fast)
        uses: actions/checkout@v2

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v5
        with:
          java-version: ${{ matrix.java }}

      - name: Install Ruby
        uses: actions/setup-ruby@v1
        with:
          ruby-version: 2.6

      - name: Common sbt setup
        run: $SBT ++${{ matrix.scala }} transferCommonResources exportSecretsForActions

      - run: ./scripts/commonSetup

      - name: Export CLONE_DIR
        run: 'echo ::set-env name=CLONE_DIR::$(mktemp -d /tmp/precog-bump.XXXXXXXX)'

      - name: Export PR_NUMBER
        run: 'echo ::set-env name=PR_NUMBER::$(echo $GITHUB_REF | cut -d''/'' -f3)'

      - run: ./scripts/checkAndAutoMerge