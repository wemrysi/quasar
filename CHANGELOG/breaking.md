- remove functions that existed as a consequence of the implementation and were unlikely to ever be called (e.g., `\`(-)\`(a, b)` for subtraction)
- extract SQL->LP mapping from StdLib
- restrict `Coalesce` to SQL²
