{
    "name": "select from a table named by a variable",

    "data": "smallZips.data",

    "variables": {
        "table": "`smallZips.data`"
    },

    "query": "select count(*) as cnt from :table where pop < 1000",

    "predicate": "exactly",
    "ignoreResultOrder": true,

    "expected": [{ "cnt": 19 }]
}
