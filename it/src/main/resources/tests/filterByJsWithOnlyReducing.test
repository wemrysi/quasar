{
    "name": "filter by JS expression, with only reducing projections",

    "backends": {
        "lwc_local":"pendingIgnoreFieldOrder",
        "mimir":"pendingIgnoreFieldOrder"
    },
    "NB": "ch893",
    "data": "largeZips.data",

    "query": "select max(pop) as max_pop, min(city) as min_city from `largeZips.data` where length(city) < 6",

    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": [{ "max_pop": 85710, "min_city": "ABAC" }]
}
