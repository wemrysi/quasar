{
    "name": "group by array",
    "data": "slamengine_commits.data",
    "query": "select distinct count(*) as cnt from `slamengine_commits.data` group by parents",
    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": [{ "cnt": 3 },
                 { "cnt": 2 },
                 { "cnt": 1 }]
}
