{
  "name": "count distinct",

  "backends": {
    "postgresql": "pending"
  },

  "data": "olympics.data",

  "query": "select count(distinct sport) as cnt from olympics",

  "predicate": "equalsExactly",

  "expected": [
    { "cnt": 7 }
  ]
}
