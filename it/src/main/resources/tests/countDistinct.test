{
  "name": "count distinct",

  "backends": {
    "postgresql": "pending",
    "marklogic":  "pending",
    "couchbase":  "pending"
  },

  "data": "olympics.data",

  "query": "select count(distinct sport) from olympics",

  "predicate": "equalsExactly",

  "expected": [
    { "0": 7 }
  ]
}
