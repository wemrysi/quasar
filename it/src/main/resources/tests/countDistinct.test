{
    "name": "count distinct",

    "data": "olympics.data",

    "query": "select count(distinct sport) as cnt from `olympics.data`",

    "predicate": "exactly",
    "ignoreResultOrder": true,

    "expected": [{ "cnt": 7 }]
}
