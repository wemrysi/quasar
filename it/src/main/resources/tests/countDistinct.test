{
    "name": "count distinct",

    "data": "demo/olympics.data",

    "query": "select count(distinct sport) as cnt from `demo/olympics.data`",

    "predicate": "exactly",
    "ignoreResultOrder": true,

    "expected": [{ "cnt": 7 }]
}
