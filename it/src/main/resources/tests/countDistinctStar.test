{
    "name": "count distinct *",
    "backends": {
        "mongodb_read_only": "pending",
        "postgresql":        "pending"
    },
    "data": "olympics.data",
    "query": "select count(distinct *) as cnt from olympics",
    "predicate": "containsExactly",
    "expected": [{ "cnt": 2304 }]
}
