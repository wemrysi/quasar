{
    "name": "count distinct *",
    "backends": {
        "mongodb_q_3_2":     "pending"
    },
    "data": "olympics.data",
    "query": "select count(distinct *) as cnt from olympics",
    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": [{ "cnt": 2304 }]
}
