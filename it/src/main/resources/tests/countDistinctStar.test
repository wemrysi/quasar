{
    "name": "count distinct *",
    "backends": {
        "mimir":      "pending",
        "postgresql": "pending"
    },
    "data": "olympics.data",
    "query": "select count(distinct *) as cnt from olympics",
    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": [{ "cnt": 2304 }]
}
