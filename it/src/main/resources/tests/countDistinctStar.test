{
    "name": "count distinct *",
    "backends": {
    },
    "data": "olympics.data",
    "query": "select count(distinct *) as cnt from `olympics.data`",
    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": [{ "cnt": 2304 }]
}
