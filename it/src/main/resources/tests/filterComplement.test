{
    "name": "filter on list complement",
    "data": "zips.data",
    "query": "select count(*) as cnt from `zips.data` where state not in (\"AZ\", \"CO\")",
    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": [{"cnt": 28669}]
}
