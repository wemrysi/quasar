{
    "name": "filter on list membership",
    "data": "zips.data",
    "query": "select count(*) as cnt from `zips.data` where state in (\"AZ\", \"CO\")",
    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": [{"cnt": 684}]
}
