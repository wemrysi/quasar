{
    "name": "qa_subset-simple-limit-and-offset",

    "backends": {
       "couchbase":         "ignoreFieldOrder",
       "marklogic_json":    "ignoreFieldOrder",
       "mimir":             "ignoreFieldOrder",
       "spark_hdfs":        "pending",
       "spark_local":       "pending"
    },

    "NB": "Pending for spark because QScript understand order (first Limit and then Offset) and spark folllows it. This query should either ignore order or not compile at all (requires change in qscript)",

    "data": "subset.data",

    "query": "select a, b from subset order by a limit 2 offset 3",

    "predicate": "exactly",
    
    "expected": [
        {"a": 4, "b": 3},
        {"a": 5, "b": 4}
     ]
}