{
    "name": "qa_subset-simple-limit-and-offset",

    "backends": {
       "couchbase":         "ignoreFieldOrder",
       "marklogic_json":    "ignoreFieldOrder",
       "mimir":             "ignoreFieldOrder",
       "postgres":          "pending"
    },

    "NB": "pending for postgres as by default postgres ignores order of offset and limit",

    "data": "subset.data",

    "query": "select a, b from subset order by a limit 3 offset 2",

    "predicate": "exactly",
    
    "expected": [
        {"a": 3, "b": 2}
    ]
}