{
    "name": "sort of a single value reduction",

    "data": "demo/olympics.data",

    "query": "select count(*) as cnt from `demo/olympics.data` order by cnt",

    "predicate": "exactly",

    "expected": [{ "cnt": 2311 }]
}
