{
    "name": "sort of a single value reduction",

    "data": "olympics.data",

    "query": "select count(*) as cnt from `olympics.data` order by cnt",

    "predicate": "exactly",

    "expected": [{ "cnt": 2311 }]
}
