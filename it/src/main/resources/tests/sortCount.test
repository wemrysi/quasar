{
    "name": "sort of a single value reduction",

    "data": "olympics.data",

    "query": "select count(*) as cnt from olympics order by cnt",

    "predicate": "exactly",
    "ignoreResultOrder": true,

    "expected": [{ "cnt": 2311 }]
}
