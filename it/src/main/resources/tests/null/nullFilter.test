{
    "name": "filter on is null",

    "backends": {
    },
    "description": "expression with `is null` should be true whenever the same filter is satisfied",

    "data": "nulls.data",

    "query": "select distinct (val is null) as expr from `nulls.data` where val is null",

    "predicate": "exactly",
    "ignoreResultOrder": true,

    "expected": [{ "expr": true }]
}
