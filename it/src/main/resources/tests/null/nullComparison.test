{
    "name": "filter on `!= null`",
    "backends": {
    },
    "data": "nulls.data",
    "query": "select distinct (val != null) as expr from `nulls.data` where val != null",
    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": [{ "expr": true }]
}
