{
    "name":  "shift map keys - nested",
    "backends": {
    },
    "data": "nested.data",
    "query": "select topObj.midObj{_:} from `nested.data`",
    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": ["botArr", "botObj"]
}
