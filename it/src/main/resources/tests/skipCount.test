{
    "name": "skip and count",

    "data": "zips.data",

    "query": "select count(*) from (select * from zips offset 10) as x",

    "predicate": "equalsExactly",
    "expected": [{ "0": 29343 }]
}
