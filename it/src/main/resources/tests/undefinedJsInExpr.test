{
    "name": "propagate undefined and null in JS",
    "backends": {
        "lwc_local":         "pending",
        "mimir":             "pending"
    },
    "data": "zips.data",
    "query": "select distinct length(meh) as meh_len, meh.mep, length(meh.mep) as mep_len from `zips.data`",
    "predicate": "exactly",
    "expected": [{}]
}
