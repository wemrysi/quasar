{
    "name": "population of Boulder",
    "data": "zips.data",
    "query": "select sum(pop) as Population from `zips.data` where city=\"BOULDER\" and state=\"CO\"",
    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": [{ "Population": 108968 }]
}
