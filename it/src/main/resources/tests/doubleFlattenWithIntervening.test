{
    "name": "double flatten with intervening field",
    "backends": {
    },
    "data": "nested.data",
    "query": "select topObj{*}.botObj{*} from `nested.data`",
    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": ["m", "n", "o"]
}
