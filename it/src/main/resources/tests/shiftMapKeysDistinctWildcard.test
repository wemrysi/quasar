{
    "name":  "shift map keys - distinct wildcard",
    "backends": {
        "marklogic_xml": "pending"
    },
    "data": "zips.data",
    "query": "select distinct *{_:} from zips",
    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": ["_id",
                 "city",
                 "loc",
                 "pop",
                 "state"]
}
