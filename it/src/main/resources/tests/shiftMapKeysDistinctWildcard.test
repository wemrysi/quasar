{
    "name":  "shift map keys - distinct wildcard",
    "backends": {
    },
    "data": "zips.data",
    "query": "select distinct *{_:} from `zips.data`",
    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": ["_id",
                 "city",
                 "loc",
                 "pop",
                 "state"]
}
