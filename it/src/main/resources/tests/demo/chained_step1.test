{
    "name": "chained_step1",
    "data": "patients.data",
    "backends": {
       "couchbase":         "ignoreFieldOrder",
       "marklogic_json":    "ignoreFieldOrder",
       "mimir":             "ignoreFieldOrder",
       "postgres":          "pending"
    },
    "NB": "Postgres connector fails due to a DeleteKey element in the reduce bucket",
    "query": "SELECT DISTINCT (state) as State FROM patients ORDER BY state ASC LIMIT 5",
    "predicate": "exactly",
    "expected": [
        { "State":  "AK" },
        { "State":  "AL" },
        { "State":  "AR" },
        { "State":  "AZ" },
        { "State":  "CA" }
    ]
}