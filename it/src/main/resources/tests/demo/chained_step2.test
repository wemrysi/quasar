{
    "name": "chained_step2",
    "data": "patients.data",
    "backends": {
       "couchbase":         "ignoreFieldOrder",
       "marklogic_json":    "ignoreFieldOrder",
       "mimir":             "ignoreFieldOrder",
       "postgres":          "pending"
    },
    "NB": "Postgres connector can't handle references to results of IncludeIds. Also, this
           whole IncludeId element seems to be redundant here.",
    "query": "SELECT DISTINCT (city) FROM patients WHERE state = \"CO\" ORDER BY city ASC LIMIT 5",
    "predicate": "exactly",
    "expected": [
      "ANTONITO",
      "ARAPAHOE",
      "ARBOLES" ,
      "ARRIBA"  ,
      "ARVADA"  
    ]
}