{
    "name": "skip and count",
    "backends": {
    },
    "data": "../zips.data",

    "query": "select count(*) from (select * from `../zips.data` offset 10) as x",

    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": [29343]
}
