{
    "name": "filter on date part, where the field isn't a timestamp",

    "backends": {
    },
    "data": "../days.data",

    "query": "select day from `../days.data` where date_part(\"dow\", epoch) >= 3",

    "predicate": "exactly",
    "ignoreResultOrder": true,

    "expected": []
}
