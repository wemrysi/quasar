{
  "name": "regex on non-string field",
  "backends": {
  },
  "data": "zips.data",
  "query": "select distinct * from `zips.data` where pop ~* \"foo\"",
  "predicate": "exactly",
  "ignoreResultOrder": true,
  "expected": []
}
