{
  "name": "regex on non-string field",
  "backends": {
  },
  "data": "zips.data",
  "query": "select distinct * from zips where pop ~* \"foo\"",
  "predicate": "exactly",
  "ignoreResultOrder": true,
  "expected": []
}
