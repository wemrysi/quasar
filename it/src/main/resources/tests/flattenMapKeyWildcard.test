{
    "name": "flatten map key - distinct wildcard",
    "backends": {
    },
    "data": "zips.data",
    "query": "select distinct *{*:} from `zips.data`",
    "predicate": "exactly",
    "ignoreResultOrder": true,
    "expected": ["_id",
                 "city",
                 "loc",
                 "pop",
                 "state"]
}
