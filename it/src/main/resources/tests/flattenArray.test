{
    "name": "flatten array",
    "backends": {
        "postgresql": "pending",
        "marklogic":  "skip",
        "couchbase":  "skip"
    },
    "data": "zips.data",
    "query": "SELECT loc[*] AS loc FROM zips LIMIT 1",
    "predicate": "containsExactly",
    "expected": [{ "loc" : -72.622739}]
}
